import React from 'react';
import { Container, AppTitle } from './styles';

const HeaderBar: React.FC = () => {
  return (
    <Container>
      <AppTitle>Corona Maps</AppTitle>
    </Container>
  );
};

export default HeaderBar;
